import React from 'react'
import { Box } from '@mui/material'
import { Outlet } from 'react-router-dom'

function Layout() {
  return (
    <Box sx={{ minHeight: "100vh", background: "linear-gradient(180dge, #0f172a 0%, #020617 100%)", pt:"64px",
    }}>
      
    </Box>
  )
}

export default Layout